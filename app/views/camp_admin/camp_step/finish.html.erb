<div class="container mt-5 ">
  <div class="col-md-6 mx-auto shadow p-5 mb-5 bg-body rounded">
    <% total_fields = current_user.total_attributes %>
    <% filled_fields = current_user.attributes.values.reject(&:blank?).count %>
    <% percentage_filled = (filled_fields.to_f / total_fields.to_f) * 100 %>
    <h3>You have completed  <%= percentage_filled.round %> % of your application.</h3>
    <p class='text-start'>Hello <%= current_user.first_name%><br/>
      Thank you for registered here.<br/><br/>
      This is your personal dashboard.You can see here the infomartion of your registration,<br/>
      Thank you again for your submission!
      <% if percentage_filled.round == 100 %>
        <%= link_to 'Confirm',root_path ,class: 'btn btn-success button mt-2', id:'pop' %>
      <% else %>
        <%= link_to 'Continue to Complete your Application',camp_admin_camp_step_index_path ,class: 'btn btn-success button mt-2' %>
      <% end %>
    </p>
  </div>
</div>
